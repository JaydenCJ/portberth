// Package version holds the single source of truth for the portberth
// version string, surfaced by `portberth version` and the JSON envelope.
package version

// Version is the semantic version of this build. Keep in sync with
// CHANGELOG.md and the go.mod header comment.
const Version = "0.1.0"
